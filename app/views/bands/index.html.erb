<h1>Bands</h1>

<%= link_to "New Band", new_band_path %>

<ul>
    <% @bands.each do |band| %>
        <li><%= link_to band.name, band_path(band) %> -- <%= link_to "Edit", edit_band_path(band)  %>
        </li>
    <%end%>
</ul>


